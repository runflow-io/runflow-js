# Changesets

Add a changeset for every user-visible change:

```bash
bun changeset
```

Changesets explain *what* changed and at *what* semver level. They get
collapsed into a release PR on merge to `main`.
